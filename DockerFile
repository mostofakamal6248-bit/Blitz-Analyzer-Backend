# Use Node 20 or 22
FROM node:20-alpine

WORKDIR /app

# Copy dependency files
COPY package*.json ./
RUN npm install 

# Copy the rest of your code
COPY . .

# Back4App defaults to port 8080, but you can change this
EXPOSE 8080

# Your start script from package.json
CMD ["npm", "start"]